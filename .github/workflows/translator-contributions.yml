name: Crowdin – Download Translations & Open PR

on:
  # schedule:
  #   - cron: '0 6 * * 1'   # Every Monday 06:00 UTC — adjust to your cadence
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  crowdin-sync:
    name: Sync translations from Crowdin
    runs-on: ubuntu-latest

    env:
      TARGET_BRANCH: locale_community

    steps:
      # ── 1. Checkout ──────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── 2. Restore last-run timestamp from cache ──────────────────────────────
      # The cache holds the ISO timestamp written at the end of the previous run.
      # Using this as dateFrom means we catch every approval since the last sync,
      # not just the few seconds since this run started.
      - name: Restore last-run timestamp
        id: cache_restore
        uses: actions/cache/restore@v4
        with:
          path: .crowdin-last-run
          key: crowdin-last-run-timestamp

      - name: Resolve sync window timestamps
        id: sync_time
        run: |
          # dateFrom — use cached value if it exists, otherwise fall back to 7 days ago
          if [ -f .crowdin-last-run/timestamp ]; then
            DATE_FROM=$(cat .crowdin-last-run/timestamp)
            echo "Using cached last-run timestamp: $DATE_FROM"
          else
            # GNU date (Linux) / BSD date (macOS) compatible fallback
            DATE_FROM=$(date -u -d "7 days ago" +"%Y-%m-%dT%H:%M:%SZ" 2>/dev/null \
              || date -u -v-7d +"%Y-%m-%dT%H:%M:%SZ")
            echo "No cache found — defaulting to 7 days ago: $DATE_FROM"
          fi

          DATE_TO=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          echo "date_from=$DATE_FROM" >> "$GITHUB_OUTPUT"
          echo "date_to=$DATE_TO"     >> "$GITHUB_OUTPUT"
          echo "Sync window: $DATE_FROM → $DATE_TO"

          # Persist current time so the next run uses it as dateFrom
          mkdir -p .crowdin-last-run
          echo "$DATE_TO" > .crowdin-last-run/timestamp

          # Snapshot the current SHA of locale_community for the diff check later
          if git ls-remote --exit-code --heads origin "$TARGET_BRANCH"; then
            echo "prev_sha=$(git rev-parse origin/$TARGET_BRANCH)" >> "$GITHUB_OUTPUT"
          else
            echo "prev_sha=" >> "$GITHUB_OUTPUT"
          fi

      # Save the updated timestamp immediately so it persists even if later steps fail
      - name: Save last-run timestamp to cache
        uses: actions/cache/save@v4
        with:
          path: .crowdin-last-run
          key: crowdin-last-run-timestamp

      # ── 3. Download translations via the official Crowdin action ─────────────
      - name: Download translations from Crowdin
        uses: crowdin/github-action@v2
        with:
          upload_sources: false
          upload_translations: false
          download_translations: true
          create_pull_request: false
          localization_branch_name: ${{ env.TARGET_BRANCH }}
          commit_message: 'chore(i18n): sync translations from Crowdin'
          github_user_name: 'crowdin-bot'
          github_user_email: 'support+bot@crowdin.com'
          project_id: ${{ secrets.CROWDIN_PROJECT_ID }}
          token: ${{ secrets.CROWDIN_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── 4. Detect whether any translation files actually changed ─────────────
      - name: Check for translation changes
        id: changed
        run: |
          git fetch origin "$TARGET_BRANCH"

          PREV="${{ steps.sync_time.outputs.prev_sha }}"
          if [ -z "$PREV" ]; then
            CHANGED_FILES=$(git diff --name-only "origin/$TARGET_BRANCH" HEAD -- \
              'translations/' 'locales/' 'i18n/' 2>/dev/null || echo "")
          else
            CHANGED_FILES=$(git diff --name-only "$PREV" "origin/$TARGET_BRANCH" -- \
              'translations/' 'locales/' 'i18n/')
          fi

          if [ -z "$CHANGED_FILES" ]; then
            echo "No translation files changed. Skipping PR."
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            echo "Changed files:"
            echo "$CHANGED_FILES"
          fi

      # ── 5. Fetch ONLY contributors active in this sync window ─────────────────
      # dateFrom = last run's timestamp, dateTo = now.
      # Only translators who did work between those two points appear in results.
      - name: Fetch active contributors from Crowdin
        id: contributors
        if: steps.changed.outputs.has_changes == 'true'
        run: |
          DATE_FROM="${{ steps.sync_time.outputs.date_from }}"
          DATE_TO="${{ steps.sync_time.outputs.date_to }}"

          echo "Fetching contributors between $DATE_FROM and $DATE_TO"

          # Request the translation-costs report scoped to this window
          REPORT_RESPONSE=$(curl -sf -X POST \
            "https://api.crowdin.com/api/v2/projects/${CROWDIN_PROJECT_ID}/reports" \
            -H "Authorization: Bearer ${CROWDIN_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "{
              \"name\": \"translation-costs\",
              \"schema\": {
                \"unit\": \"words\",
                \"currency\": \"USD\",
                \"languageId\": \"all\",
                \"userIds\": [],
                \"format\": \"json\",
                \"dateFrom\": \"$DATE_FROM\",
                \"dateTo\":   \"$DATE_TO\",
                \"groupBy\":  \"user\"
              }
            }")

          REPORT_ID=$(echo "$REPORT_RESPONSE" | jq -r '.data.identifier')
          echo "Report ID: $REPORT_ID"

          # Poll until finished (max ~90 s)
          for i in $(seq 1 18); do
            sleep 5
            STATUS=$(curl -sf \
              "https://api.crowdin.com/api/v2/projects/${CROWDIN_PROJECT_ID}/reports/${REPORT_ID}" \
              -H "Authorization: Bearer ${CROWDIN_TOKEN}" \
              | jq -r '.data.status')
            echo "  poll $i: $STATUS"
            [ "$STATUS" = "finished" ] && break
          done

          # Download the report
          DOWNLOAD_URL=$(curl -sf \
            "https://api.crowdin.com/api/v2/projects/${CROWDIN_PROJECT_ID}/reports/${REPORT_ID}/download" \
            -H "Authorization: Bearer ${CROWDIN_TOKEN}" \
            | jq -r '.data.url')

          curl -sf "$DOWNLOAD_URL" -o contributors_report.json

          # Build Co-authored-by lines — only users in this date window
          CO_AUTHORS=$(jq -r '
            .data[]
            | select(.user != null)
            | .user.fullName as $name
            | .user.username  as $user
            | "Co-authored-by: \($name) <\($user)@users.noreply.github.com>"
          ' contributors_report.json | sort -u)

          if [ -z "$CO_AUTHORS" ]; then
            CO_AUTHORS="Co-authored-by: Crowdin Community <crowdin@noreply.com>"
          fi

          echo "Contributors:"
          echo "$CO_AUTHORS"

          {
            echo "co_authors<<EOF"
            echo "$CO_AUTHORS"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
        env:
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_TOKEN: ${{ secrets.CROWDIN_TOKEN }}

      # ── 6. Amend the commit with Co-authored-by trailers ─────────────────────
      - name: Amend commit with Co-authored-by
        if: steps.changed.outputs.has_changes == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin "$TARGET_BRANCH"
          git checkout "$TARGET_BRANCH"

          # Git trailer spec: subject line, blank line, then Co-authored-by lines
          COMMIT_MSG="chore(i18n): sync Crowdin translations [locale_community]

          ${{ steps.contributors.outputs.co_authors }}"

          git commit --allow-empty --amend -m "$COMMIT_MSG"
          git push --force-with-lease origin "$TARGET_BRANCH"

      # ── 7. Open / update the Pull Request ────────────────────────────────────
      - name: Create or update Pull Request
        if: steps.changed.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ env.TARGET_BRANCH }}
          base: main                          # ← change if your default branch differs
          title: '🌐 Crowdin translation sync – community contributions'
          body: |
            ## 🌐 Crowdin Translation Sync

            This PR contains translations approved in Crowdin between
            `${{ steps.sync_time.outputs.date_from }}` and `${{ steps.sync_time.outputs.date_to }}`.

            ### Active translators in this batch

            Only contributors who made changes in this window are credited via
            `Co-authored-by:` in the commit:

            ```
            ${{ steps.contributors.outputs.co_authors }}
            ```

            ---
            *Generated by the **Crowdin Translations** workflow · branch `locale_community`*
          labels: |
            translations
            community
            crowdin
          draft: false
