from django.contrib import admin
from climweb.pages.publications.models import PageView

# The PageView model is already registered in publications.admin
# This import ensures it's available for product page views as well
